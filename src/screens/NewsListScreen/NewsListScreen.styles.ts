import { StyleSheet } from "react-native";

export default StyleSheet.create({
  container: {
    flex: 1
  },
  
  listFooter: {
    textAlign: "center",
    marginTop: 10,
    marginBottom: 50,
  },
});
